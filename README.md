# Fraud-Detection
Developed a Machine Learning–based financial fraud detection systemusing Python, achieving94% accuracy on 6M+ records. Handled class imbalance and trained a Logistic Regression model, deployed via an interactive Streamlit web app.

#First install the Requirements:(requirement.txt)
pandas,matplot,seaborn,scikitlearn,streamlit,joblib

#Run the jupter notes(analysis_model.ipynb)
One file will be add like --(fraud_detection_model.pkl)

#run this in the terminal:
streamlit run fraud_detection.py

#click on the address link-
give the inputs and click the predict button
output will be printed

